{% extends 'base.html.twig' %}

{% block title %}New Post{% endblock %}

{% block body %}
    <h1>Create new Post</h1>

    {{ include('role/_form.html.twig') }}

    <a href="{{ path('role') }}" class="btn btn-info">Back to list</a>
{% endblock %}